import React from "react";

const CityInfo = () => {
  return (
    <div>
      Here will be rendered the location in map for searched city and other
      informations .
    </div>
  );
};

export default CityInfo;
